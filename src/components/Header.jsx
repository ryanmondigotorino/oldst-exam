/**
 * Header component across the app
 * 
 * @returns React Component
 */

const Header = () => {
  return (
    <header className="header">
      <div className="container">
        <h1>Products Grid</h1>
      </div>
    </header>
  );
};

export default Header;
